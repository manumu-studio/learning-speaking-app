// PinBadge types — internal to AnnotatedTranscript folder, not exported from index.ts
import type { SentenceAnnotation } from '@/lib/text/annotationTypes';

export interface PinBadgeProps {
  annotation: SentenceAnnotation;
}
