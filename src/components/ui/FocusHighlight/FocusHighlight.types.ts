// Type definitions for the FocusHighlight component
export interface FocusHighlightProps {
  metricLabel: string;
  currentScore: number;
  previousScore: number | null;
  animationDelay?: number;
}
