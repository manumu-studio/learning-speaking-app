// Prop types for the LibraryPromptCard grid card component
import type { LibraryPrompt } from '@/lib/prompts/promptLibrary.types';

export interface LibraryPromptCardProps {
  prompt: LibraryPrompt;
}
