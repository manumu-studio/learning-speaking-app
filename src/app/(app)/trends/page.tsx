// Trends page — server component rendering TrendsPage (auth handled by (app) layout)
import { TrendsPage } from '@/features/trends/TrendsPage';

export default function TrendsRoute() {
  return <TrendsPage />;
}
