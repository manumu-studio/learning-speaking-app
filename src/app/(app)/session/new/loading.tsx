// Loading state for new session page — pulsing microphone during route transition
import { MicLoadingIndicator } from '@/components/ui/MicLoadingIndicator';

export default function NewSessionLoading() {
  return <MicLoadingIndicator />;
}
