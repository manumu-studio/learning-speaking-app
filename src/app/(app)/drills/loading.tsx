// Loading state for drills page — pulsing microphone during data fetch
import { MicLoadingIndicator } from '@/components/ui/MicLoadingIndicator';

export default function DrillsLoading() {
  return <MicLoadingIndicator />;
}
