// Loading state for drill exercise page — pulsing microphone during route transition
import { MicLoadingIndicator } from '@/components/ui/MicLoadingIndicator';

export default function DrillLoading() {
  return <MicLoadingIndicator />;
}
